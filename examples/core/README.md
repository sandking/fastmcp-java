# FastMCP Core Example

This example module keeps the plain Java examples compile-checked by Maven.

Run it from the repository root:

```bash
mvn -Pexamples test
```

This module is for usage checks only and is skipped during Maven deploy.
